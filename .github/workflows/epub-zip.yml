name: Zip EPUBs
on: push
jobs:
  epub-zip:
    runs-on: ubuntu-latest
    steps:
      # Checkout the branch
      - name: checkout main
        uses: actions/checkout@v2
        with:
          path: main

      - name: zip
        run: |
          cd main
          pwd
          ls -la
          chmod +x .github/workflows/deterministic-zip
          sudo mv .github/workflows/deterministic-zip /usr/local/bin/deterministic-zip
          sudo mv .github/workflows/update_json.py /usr/local/bin/update_json.py
          find . -maxdepth 2 -mindepth 2 -not -path '*/.*' -type d -exec bash -c "cd \"{}\" && cd foreign && deterministic-zip -r ../foreign.epub * && cd .. && rm -rf foreign && cd english && deterministic-zip -r ../english.epub * && cd .. && rm -rf english" \;
          rm -rf .github book_information.json LICENSE README.md .git
          cd ..
          
      - name: checkout zip
        uses: actions/checkout@v2
        with:
          ref: zip
          path: zip
          
      - name: commit zip
        run: |
          cp -r main/* zip/
          cd zip
          git diff --name-only > ../changes
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"
          git add --all
          git commit -m "automated: zipping into .epub"
          git push --force --set-upstream origin zip
          last_commit=$(git rev-parse HEAD)
          commit_time=$(git show -s --format=%ct $last_commit)
          cd ..
          rm -rf main
          
      - name: checkout main again
        uses: actions/checkout@v2
        with:
          path: main
          
      - name: update book
        run: |
          cd main
          python update_json.py ../changes $last_commit $commit_time book_information.json
          git add book_information.json
          git commit -m "automated: updating book_information.json"
          git push
          
